import { ToxicityClassifier } from './toxicity-classifier';

describe('ToxicityClassifier', () => {
  it('should create an instance', () => {
    expect(new ToxicityClassifier()).toBeTruthy();
  });
});
